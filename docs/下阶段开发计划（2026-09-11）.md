# 河池政协网 · 下阶段开发计划

> 编制日期：2026 年 9 月 11 日
> 编制依据：本仓库代码与文档、`/Users/wangyuehua/Documents/网站制作与迁移及相关服务/政协/` 下的合同、方案、执行时间表、交接文档、备案材料，以及 `zhengxie2026/` 下的旧站服务器与数据库快照（均为本次实际读取）。
> 文末附事实／推断／未知备注，正文数字均标注来源。

## 一、进度盘点（截至 2026-09-11）

### 1.1 已完成

| 项 | 现状 | 来源 |
| --- | --- | --- |
| 仓库 | `github.com/wangyuehua0429/hechi-zhengxie`，`origin/main` 停在 `631d055`（2026-09-11 16:10），本地 `main` 另有内页回归修复与本次文档更新（尚未推送）；累计 35 次提交 | `git log`／`git status` |
| 前端静态版首页 | `frontend/home/index.html` 312 行、`css/style.css` 765 行、`js/main.js` 859 行、`data/home.json` 50,778 字节（21 个顶层数据键、18 个主栏目） | 实际读取文件 |
| 内页模板全集 | `channel.html` 117 行、`detail.html` 124 行、`css/inner.css` 763 行、`js/{shell,channel,detail}.js` 共 1,152 行；覆盖 `list`／`leaders`／`about`／`county`／`gallery`／`video`／`topic`／`interactive` 八类版式（`about` 暂无栏目使用） | 实际读取文件 |
| 内页样例数据 | `data/channel.json` 43 个栏目（31 个带列表，共 432 条）、`data/article.json` 70 篇正文（正文图片 53 张、附件 1 条） | 数据文件解析 |
| 图片来源 | 首页 48 个 `img` 字段、栏目列表 75 张缩略图、正文 53 张配图全部为本地相对路径；`images/channel/` 138 张、`images/remote/` 44 张，`frontend/home/images/` 合计 17 MB | `frontend/home/data/*.json` 解析 + 目录统计 |
| 旧站链接映射 | 210 个 `url` 字段中 184 个指向旧站，其中 95 个可在运行时改写为新版内页，89 个仍落旧站（71 个 `news_view.php` 详情、8 个站点首页／根路径、6 个 `news-view-<id>.html` 静态页、1 个未入样例的栏目 `news_list.php?id=210`、3 个旧站专题目录）；为提速首屏新增 4.4 KB 的 `data/channel-index.json` 精简索引 | 脚本统计 + `js/site-links.js` 实测 |
| 在线预览 | GitHub Pages 可访问：`https://wangyuehua0429.github.io/hechi-zhengxie/` 跳转至新首页，首页／栏目页／详情页均 HTTP 200；线上内页同样存在下述回归 | 本机 `curl` + Playwright 实测（2026-09-11） |
| 首页渲染实测 | 桌面 1440 与手机 390 两档：66 张图片慢速滚动后全部加载成功（`naturalWidth > 0`）、无横向溢出、控制台 0 报错 0 警告 | Playwright 实测（2026-09-11） |
| 文档 | `README.md`、`docs/架构与实施说明.md`、`frontend/home/README.md` 本次按本节结论同步更新，写明当前进度、数据契约与分期范围 | 本次实际读取与改写 |

提交节奏：9 月 5 日 9 次（首次建版）、9 月 8 日 2 次、9 月 11 日 23 次，累计 35 次（含回归修复与本次文档更新）。9 月 11 日的工作重心已由首页视觉转向内页形态与站头交互——当日提交里 17 次的说明落在“内页”或“站头”，16:03 的首屏加载性能优化之后，16:10 换了一版首屏左一条幅图（`631d055`，提案填报系统），随后修复了精简索引引发的内页回归（`b95ccf8`）。

**当日回归已修复（`b95ccf8`）**：提交 `85918c2` 把链接映射数据源换成 4.4 KB 精简索引（只有 `type` 与 `ids`）后，`js/shell.js` 把这份索引当作栏目数据转发给 `channel.js`／`detail.js`，导致 `channel.html` 全部栏目页报“数据加载失败”（列表与分页不渲染）、`detail.html` 标题栏目名显示 `undefined`。修复方式：`shell.js` 的 `channelsReady` 独立取完整 `data/channel.json`，精简索引只留给链接改写（内页本就需要完整数据，请求数不变；首页首屏仍只取索引）。修复前后用同一套 Playwright 检查对照验证：修复前 `FAIL`（三档栏目页失败、详情页标题含 `undefined`），修复后 `PASS`（`channel.html?id=904` 首页列 20 行 2 页、`?id=202` 10 张领导照片、`?id=314` 20 张图集卡片、`detail.html?id=62180` 标题带“政协动态”），桌面 1440 与手机 390 两档无溢出、无破图、控制台 0 报错。

### 1.2 尚未开始（仓库内为空骨架）

`backend/`（`config`／`public`／`resources`／`routes`／`src`／`templates` 均只有 `.gitkeep`）、`api/`、`services/proofreader/`（`app`／`tests`／`data`）、`database/`（`migrations`／`seed`）、`tools/migrate/`、`tests/` —— 全部无实现代码。

即：**后端、数据库新模型、内容 API、静态化发布器、迁移工具、编校微服务均为零行代码。**

> 2026-09-11 补充：按“前端二级页与详情页 → 后端 → 历史数据接入”的新顺序，当日已把内页推进到模板全集（栏目页 + 详情页 + 八类版式 + 43 个栏目、70 篇详情样例数据 + 站头收窄与站内链接映射），详见第三节阶段 A。截至该时点，后端仍为零行代码。

> 2026-09-11 晚补充：**后端最小骨架已落地**，本节上述盘点随之作废。已完成 `backend/bin/{migrate,seed,publish}.php`、`backend/public/index.php`、`backend/routes/api.php`、`backend/src/{Api,Http,Repository,Publish,Support}`、`database/migrations/{mysql,sqlite}/001_init.sql`、`backend/templates/page.php`、根目录 `docker-compose.yml` 与 `deploy/`、接口契约 [api-contract.md](api-contract.md)。本机以 SQLite 实测：建表 17 张、灌入 43 个栏目／406 篇稿件／425 条栏目归属／53 张正文图／2 条附件／21 个首页模块，接口与发布器对拍 39 项全部通过（`node tests/api-check.mjs`），前端页面回归检查 19 项全部通过（`node tests/check-pages.mjs`）。仍为零行代码的只剩 `services/proofreader/`（编校微服务）与 `tools/migrate/`（旧库迁移，属阶段 D）；后台管理界面与权限、附件上传、301 生成同属待做。

### 1.3 素材与数据资产（本地存在，未纳入 git）

| 资产 | 规模／状态 | 路径 |
| --- | --- | --- |
| 旧站静态文章页 | 53,542 个文件，513 MB | `zhengxie2026/gxhczx.gov.cn/html/`（`news-view-<id>.html`） |
| 旧站程序与图片 | PHP 脚本 + `uploadfile`／`uploadfiles` | `zhengxie2026/gxhczx.gov.cn/`、`da.gxhczx.gov.cn/`、`gxhc.gxhczx.gov.cn/` |
| 旧库 SQL 导出 | 73,795,613 字节；导出时间 2026-04-28 10:58；MySQL 5.7.44；16 张表 | `zhengxie2026/date/database/gxhczx_db.sql` |
| 旧库在线数据文件 | `rd_news.MYD` 71,208,372 字节，最后修改 2026-09-05 13:28（**比 SQL 导出新 4 个多月**） | `zhengxie2026/server/data/gxhczx_db/` |
| 面板每日备份 | 478 个 zip，最新 2026-09-05（含各站 Nginx vhost 配置） | `zhengxie2026/date/panel/` |
| 服务器配置副本 | Nginx、MySQL 5.7、PHP、计划任务、面板 | `zhengxie2026/server/` |
| 合同与方案 | 合同编号 HCZX-2026-001（9 月 3 日版）、开发思路与技术栈方案、执行时间表、交接文档、ICP 备案材料 | 政协目录根 |

### 1.4 旧库数据画像（本次实读 SQL 统计）

| 表 | 行数 | 说明 |
| --- | --- | --- |
| `rd_news` | 20,705（已审 20,540 / 未审 163） | 文章主表 |
| `rd_menu` / `rd_menu1` | 284 / 291 | 栏目树 |
| `rd_hot` | 3,193 | 焦点图片 |
| `rd_run` | 603 | 热点关注（领导活动流水） |
| `rd_cr` | 106 | 领导简介 |
| `rd_links` | 30 | 友情链接 |
| `rd_region` | 14 | 县区／排名 |
| `rd_video` | 4 | 视频 |
| `rd_ad` / `rd_hudong` / `rd_admin` / `rd_level` / `rd_about` | 8 / 7 / 9 / 3 / 1 | 广告、互动、管理员、层级、简介 |
| `rd_pt_news` / `rd_pt_admin` | 0 / 1 | 稿件互通相关表（本期预留） |

`rd_news` 年份分布：2021 年 623、2022 年 3,319、2023 年 5,734、2024 年 4,540、2025 年 4,964、2026 年 1,523（截至 4 月 28 日导出时点）。表内 `Type` 字段共 98 个不同取值，最大几类为 306 时政要闻 13,198 条、5004 都安热线 2,218 条、905 1,293 条、904 839 条、314 图片新闻 648 条。

主表字段为 `ID / Title / Title1 / Title2 / Pic / Word / Audit / Region / Hot / Top / Type / Num / Order / Time / From / Author / Edit`，MyISAM 引擎，`AUTO_INCREMENT = 62247`。

### 1.5 与计划节点的对照

执行时间表（9 月 1 日版）规定 9 月 10 日至 14 日为 T3 设计期：视觉定稿、栏目页／详情页模板、新云环境部署。今天是 T3 第 2 天，实际状态为：首页视觉已多轮打磨，**栏目页与详情页模板已出全集并跑通样例数据（16:03 优化提交引入的回归已于当日修复，见 1.1），新云环境部署仍无任何证据**。

合同（9 月 3 日版）关键节点：9 月 30 日前完成自查迁出（新云选型、环境适配、全量数据备份、新站上线试运行）；10 月 1 日至 31 日新旧并行与 ICP 备案变更；11 月 20 日起递交《政务云资源回收申请》并正式关停。

按今天计算：距 9 月 30 日合同节点 **19 天**，距 11 月 20 日关停 **70 天**。若沿用 9 月 1 日时间表的 10 月 8 日试运行口径，则只剩 27 天。

2026-09-11 调整后的推进顺序为“前端二级页与详情页 → 后端 → 历史数据接入”，原“设计期—开发期—联调期”的三段划分相应改为按交付面划分，见第三节。

## 二、须先行澄清的口径冲突（建议 9 月 12 日前完成）

以下 6 项在不同文件间口径不一致，直接影响开发范围，必须先由甲方或融媒体中心确认后再冻结需求，否则返工不可避免。

> 逐项确认材料见 [需求冻结确认清单（2026-09-12）.md](需求冻结确认清单（2026-09-12）.md)。
> **2026 年 9 月 12 日记录口径**：事项一 A（11 月 20 日关停，合同口径）、事项二 B（本期不按信创适配）、事项三 B（历史图片、附件与下载文件纳入迁移）、事项四 A（编校与主站同步建设）、事项五 B（对外域名 `www.gxhczx.gov.cn`）、事项六待定。提示：事项二、三、五与合同或备案材料所载口径不一致，需书面报备或办理相应变更；事项四使编校服务须在本期内交付，而编校厂商仍为四选一待定。

| 序号 | 冲突点 | A 口径 | B 口径 |
| --- | --- | --- | --- |
| 1 | 政务云停服日 | 合同：11 月 20 日起关停、11 月底到期 | 开发思路方案与执行时间表：11 月 1 日停服 |
| 2 | 信创适配范围 | 合同：11 月底前完成应用系统信创适配开发与测试并部署至移动云 | 开发思路方案：选用移动云、本期不按信创适配 |
| 3 | 历史图片／附件／下载文件 | 合同：不在本次迁移范围内 | 开发思路方案与架构说明：纳入迁移 |
| 4 | 编校模块工期 | 合同与方案：与主站同步建设（首年费用已含一次性 3 万元） | 执行时间表：最早 2027 年 1 月满足启动条件、预计 2027 年 2 月启动 |
| 5 | 对外域名与备案主体 | 备案材料：网站域名 `gxhcrm.cn`，备案 IP `36.134.86.175 / 36.134.86.167 / 36.134.87.100` | 现网与首页数据：域名 `www.gxhczx.gov.cn` |
| 6 | 预发布环境 | 服务器面板备份显示已存在 `new.gxhczx.gov.cn` 站点（root `/www/wwwroot/new.gxhczx.gov.cn`，PHP 7.4，配置时间 2026-04-13） | 仓库内无该环境任何记录，用途与归属待确认 |

其中第 1、2、3 项决定工作量与合规边界，属最高优先级；第 4 项决定是否需要在本期内并行开发编校服务；第 5、6 项决定上线与解析方案。

## 三、下阶段开发计划

总体思路：按 2026-09-11 确定的三段顺序推进——**前端二级页与详情页 → 后端 → 历史数据接入**。前端先把 17 个一级栏目与全部内页形态定稿，后端按定稿模板建数据模型与发布链路，最后一次性接历史数据与 301 映射。9 月 30 日合同节点、11 月 20 日关停节点作为硬约束倒排。

### 阶段 A：前端二级栏目页与详情页（9 月 11 日—9 月 18 日，进行中）

已完成（9 月 11 日，按提交先后）：

1. 二级栏目页与详情页静态原型落地：`605735c` 新增 `frontend/home/channel.html`、`detail.html` 与配套 `css/inner.css`、`js/shell.js`、`js/channel.js`、`js/detail.js`。
2. 栏目版式与样例数据扩充：`5371db6` 改为政务经典列表版式，`5593604` 扩展到主导航全栏目并把样例数据扩到 43 个栏目页、70 篇详情。
3. 栏目形态补齐：`58e99b2` 一次补齐 `list`（左侧栏目按钮 + 最新新闻 + 图片新闻）、`leaders`（政协领导，按职务分组的照片卡片）、`about`（一页式）、`county`（县（区）政协，面板上方为“全部”与各县区站点入口）、`gallery`、`video`、`topic`、`interactive` 八类版式；外链型（政协文史馆）不做内页，只做跳转。
4. 首页接入新内页：`bd7f0ce` 后首页栏目入口改指新版内页，`js/site-links.js` 统一改写旧站栏目与稿件地址，首页（`js/main.js`）与内页（`js/shell.js`）共用同一映射表。
5. 侧栏与内页细节：`8a2990d` 侧栏按右栏高度自动配平（图片新闻默认 2 行 4 张，可收为 1 行 2 张或扩为 3 行 6 张；最新新闻 3—30 条，右栏过短时隐藏图片新闻卡），`e8e7e8f` 切换左侧栏目保持浏览位置，`4413940` 面包屑去掉白底横条，`ee75904` 领导卡片去掉“简介”链接，`99afcf6` 首页导航名“政协概况”与栏目页标题“政协领导”各归其位。
6. 站头收窄与动画：`1aed73e`／`016b4da` 内页站头收窄至约三分之一并保留站名标志，`4bec985` 返回首页时标志放大与站头下移同帧起步，`b3afd16` 首页展开改为纯 transform 动画。
7. 首屏性能：`85918c2` 站头背景图设 `fetchpriority`、logo 与次级背景图异步解码，`.nav-wrap` 预留 90px 高度消除主导航异步渲染带来的抖动与展开等待，并批量重压缩略图；新增 `data/channel-index.json`（4.4 KB）作为链接映射的精简索引。
8. 回归修复：`b95ccf8` 把 `js/shell.js` 的 `channelsReady` 改回取完整 `data/channel.json`，修掉 `85918c2` 精简索引导致的内页故障，精简索引只留链接改写用途（详见 1.1“当日回归已修复”）。
9. 实测：桌面 1440 与手机 390 两档，首页 66 张图片全部加载成功、无横向溢出、控制台 0 报错 0 警告；`favicon.ico` 已随内页补齐。

待办：

1. 栏目映射补全：旧库 98 个 `Type` 取值目前只映射出 43 个栏目页，17 个一级栏目入口已全覆盖，仍需把其余二级栏目与专项 `Type` 逐条落到 `channel_id`。
2. 一页式 `about` 版式已实现但暂无栏目使用（政协章程、机构设置、政协简介等待挂），需确认哪些栏目走一页式。
3. 旧站链接收尾：210 个 `url` 字段中仍有 89 个落旧站（71 个 `news_view.php` 详情、8 个站点首页／根路径、6 个 `news-view-<id>.html` 静态页、1 个未入样例的栏目、3 个旧站专题目录），需在样例数据补齐或接入全量数据后回归到 0。
4. 分页与样例范围：列表分页目前只覆盖每栏目 24 条样例，页脚统计显示旧库全量条数，需在后端接入后改为真实分页。

交付物：内页模板全集、栏目参数总表、两档响应式自测记录（均已于 9 月 11 日产出，栏目页回归修复后再补一次全站自测）。

### 阶段 B：口径冻结与基线固化（9 月 11 日—9 月 12 日，与 A 并行）

1. 与甲方确认第二节 6 项口径，形成《需求冻结确认单》并双方签字。
2. 从 `172.21.43.11` 重新导出一次全量数据库（现有 SQL 停留在 4 月 28 日，在线数据文件已到 9 月 5 日），同时备份 `uploadfile`／`uploadfiles` 与 `html/` 静态目录。
3. 冻结栏目总表：以 `rd_menu`／`rd_menu1` 为准梳理 17 个一级栏目与二级子栏目，产出 `Type`（98 个取值）到新 `channel_id` 的完整映射表。
4. 固化旧 URL 清单：`news-view-<id>.html`（53,542 个）与 `news_list.php?id=<n>`、`cq_view.php?id=<n>`、`qy_list.php` 等动态路径，作为 301 映射输入。

交付物：《需求冻结确认单》《栏目与 Type 映射总表》《旧 URL 清单》。

### 阶段 C：后端开发（9 月 19 日—9 月 26 日）

1. 新库建模：`sys_site`、`sys_channel`、`cms_article`、`cms_attachment`、`sys_url_redirect`、`sys_user`、`sys_role`、`sys_operation_log`，以及编校扩展字段与本地词库表。
2. PHP 轻量 CMS：登录与权限、栏目管理、文章增删改查、附件上传、草稿／已发布／下线三态。
3. 全文静态化发布器：首页、栏目列表、文章详情生成静态 HTML，内容变更触发增量刷新，支持站点地图与分页。模板按阶段 A 定稿的内页结构实现。
4. 内容 API（契约先行）：`/api/v1/channels`、`/articles`、`/article/{id}`、`/attachments`、`/search`，字段与阶段 A 的 `channel.json`／`article.json` 契约对齐，供前台、后台与编校共用。
5. 用样例数据打通前后端：首页与内页从静态 JSON 切到 API，页面结构与渲染逻辑不变。

交付物：可运行的后端与静态化发布器、API 契约文档、样例数据下的完整站点。

### 阶段 D：历史数据接入（9 月 27 日—9 月 30 日）

1. 迁移脚本（`tools/migrate/`，可重复执行、幂等、带校验）：
   - `rd_news` 20,705 条 → `cms_article`，时间戳转 `datetime`，`Audit` 映射发布状态，`Type` 映射 `channel_id`；
   - `rd_menu`／`rd_menu1`／`rd_cr`／`rd_hot`／`rd_run`／`rd_links`／`rd_region`／`rd_video`／`rd_about`／`rd_ad` 等按栏目与模块分别落库；
   - 正文清洗：去除 `font`／`span` 内联样式、统一图片路径、剥离旧站冗余空段落；
   - 来源字段清理：约 7.8% 记录的 `From` 混入日期版面（如“河池日报 2026/3/2 1 版”），需拆分回日期与版面；
   - 生成 `sys_url_redirect` 映射记录。
2. 历史图片与附件：按合同口径确认是否纳入本次迁移（见第二节第 3 项冲突）。
3. 迁移后核对：行数比对、301 命中率抽样、栏目归属抽样、移动端抽查。

交付物：迁移脚本与《迁移核对报告》、301 映射表、旧站数据备份归档。

### 阶段 E：新站上线试运行（9 月 30 日，合同节点）

1. 部署至移动云正式环境，按确认后的域名方案完成解析切换。
2. 提交《迁移实施报告》《数据核对报告》，完成合同约定的“自查迁出”。

### 阶段 F：并行校验与备案（10 月 1 日—10 月 31 日）

1. 新旧站并行，重点校验 301 跳转命中率、站内检索、附件下载、移动端与微信内置浏览器表现。
2. ICP 备案变更：营业执照、法人及网站责任人证件、真实性核验单、域名证书等材料已备齐（`备案申请资料/`），按确认后的域名与 IP 提交。
3. 等保二级配套：WAF、防篡改、主备与异地备份策略、操作日志。

### 阶段 G：智能编校模块与旧云关停（11 月 1 日—11 月 20 日）

1. 编校服务：接口契约先行 → 厂商 API 对接（讯飞／爱校对／人民智文／蜜度四选一，待定）→ 后台编辑器右侧窗口与顶部标签页嵌入 → 本地词库建设（领导、行政区划、忌讳词、本地提法四类，支持热更新）→《智能编校单》PDF／Word 生成与归档。
2. 单稿编校响应目标 ≤10 秒；涉密稿件不发外网、不调用厂商接口。
3. 递交《政务云资源回收申请》，正式关停原政务云资源。

### 阶段 H：验收与移交（11 月 21 日起）

试运行数据观察、性能调优、功能验收、运维交接（含 7×24 监控、月度运维报告机制）。

## 四、风险与依赖

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| 6 项口径未确认 | 范围反复，返工 | 阶段 A 内必须闭环，未确认项按合同口径先行、同步书面向甲方报备 |
| 新云选型与开通依赖甲方或上级审批 | 阻塞阶段 B、E | 今日启动申请，先用本地／临时环境并行开发，环境就绪后平移部署 |
| 最新全量数据未取到 | 迁移丢数据 | 阶段 A 内完成重新导出，迁移前后做行数与抽样校验 |
| 前端无自动化回归检查 | 破坏性回归只能靠人工发现——`85918c2` 让全部栏目页打不开，当天未被察觉 | 按第五节第 6 条在 `tests/` 补无头检查，纳入提交前必跑 |
| 一页式 `about` 版式已实现但无栏目使用 | 政协章程、机构设置、政协简介等栏目无归属，上线前仍需返工 | 阶段 A 内确认走一页式的栏目清单并挂上数据 |
| 98 个 `Type` 值映射错误 | 文章错栏、旧链接 404 | 生成映射总表后由甲方确认，迁移后抽样回访 |
| 历史附件归属未定 | 图片失效 | 若按合同不迁移，需确认旧站图片持续可访问或改为本地留存方案 |
| 等保二级与备案周期不可控 | 影响 10 月并行窗口 | 材料已备齐，10 月 1 日即提交 |
| 仓库无 CI 与测试 | 回归靠人工 | `tests/` 补迁移校验与链接巡检，提交前跑首页渲染检查 |

## 五、建议同步落地的工程动作

1. 目录落地：~~把 `backend/`、`database/`、`tools/migrate/` 从空骨架填充为可运行的最小工程，附本地 `docker-compose`（Nginx + PHP 8 + MySQL 8 + Redis）便于多人并行。~~ **2026-09-11 晚：`backend/`、`database/` 与 `docker-compose`／`deploy/` 已完成最小可运行版本**（本机用 SQLite 跑通；Docker 编排未在本机验证），`tools/migrate/` 仍待阶段 D 落地。
2. 分支策略：`main` 保持可发布，功能分支用 `feat/`、修复用 `fix/`，提交说明沿用“类型（作用域）：描述”格式。
3. 文档补齐：`docs/api-contract.md`（接口契约）、`docs/data-mapping.md`（旧库到新库字段映射）、`docs/url-301.md`（跳转映射规则）。
4. 备份纪律：旧站素材（`zhengxie2026/` 约 8 GB）不入 git，另存外部存储并生成校验清单。
5. 前端遗留（已完成）：`favicon.ico` 已随内页放入 `frontend/home/`（首页与内页共用）；首页导航已接入新内页，“栏目入口仍跳旧站”的旧欠账已清。剩余 89 处旧站链接待样例数据补齐后回归（见 1.1 表“旧站链接映射”行）。
6. 回归防护：本日 `85918c2` 的回归说明前端缺自动化检查。建议在 `tests/` 加一条无头检查——按 `git diff` 涉及的前端页面起本地服务，断言首页/栏目页/详情页无 `undefined`、无“数据加载失败”、关键容器非空，纳入提交前必跑。

## 附：事实／推断／未知备注

**事实（本次会话实际读取）**

1. 仓库提交、分支与同步状态、工作区未提交改动、累计 33 次提交与单日提交分布：`git log`／`git status`／`git rev-list --count` 输出（2026-09-11）。
2. 首页与内页文件行数、四份数据快照的体积与统计（43 个栏目／31 个带列表／432 条列表／70 篇详情／53 张正文图／1 条附件／43 个栏目 + 432 个稿件 id）、本地图片数量与体积：实际读取 `frontend/home/` 文件后统计。
3. 在线预览与本地实测：GitHub Pages 根 `index.html`、`frontend/home/index.html`、`channel.html?id=904`、`detail.html?id=62180`、`data/channel-index.json` 均 HTTP 200（`curl`，2026-09-11）；本地 `python3 -m http.server` + Playwright 实测首页 1440／390 两档 66 张图片全部加载成功、无横向溢出、控制台 0 报错 0 警告。
4. 内页回归的复现与修复验证（2026-09-11，同一套 Playwright 检查脚本）：修复前 `85918c2` 与 `631d055` 两版上，`channel.html`（`?id=904`／`?id=202`／`?id=314`／无参数）均显示“数据加载失败”、列表 0 行，`detail.html?id=62180` 标题为 `…· undefined · 广西河池政协网`，GitHub Pages 线上同址表现一致；`85918c2^` 对照实测栏目页渲染 20 行稿件、详情页标题带“政协动态”。修复（`b95ccf8`）后重跑同一脚本：`?id=904` 首页列 20 行、分页 2 页，`?id=202` 10 张领导照片与 20 个简介链接，`?id=314` 20 张图集卡片，详情页标题带“政协动态”，桌面 1440 与手机 390 两档无溢出、无破图、控制台 0 报错。
5. 旧站链接映射覆盖率（210 个 `url` 字段、184 个旧站、95 个可改写、89 个仍落旧站）与图片重压前后体积：`git show 85918c2 --stat`、`data/home.json` 解析、目录文件尺寸统计（2026-09-11）。
6. 旧库 16 张表行数、`rd_news` 年份分布、98 个 `Type` 取值、主表字段：本次解析 `gxhczx_db.sql` 统计（导出时间 2026-04-28 10:58，MySQL 5.7.44）。
7. 静态文章页 53,542 个、513 MB，在线数据文件修改时间 2026-09-05，面板备份 478 个 zip：目录列举与文件属性。
8. 合同节点、费用、编校交付要求，执行时间表 T1—T7：实际读取对应 docx。
9. 备案材料中的域名与 IP、交接文档中的服务器配置与账号入口：实际读取对应 xlsx／docx。
10. `new.gxhczx.gov.cn` 站点配置：`date/panel/2026-09-05.zip` 内 `vhost/nginx/new.gxhczx.gov.cn.conf`。

**推断**

1. 旧站多站共库还是分库：由 7 站同服与数据文件布局推断，未登录数据库确认。
2. 栏目页回归的成因：由 `85918c2` 的 diff（`js/site-links.js` 改读精简索引、返回结构随之变化）、`js/shell.js` 的 `channelsReady` 赋值与 `85918c2^` 的对照实测三者共同推断，未做源码级断点确认；修复按该判断实施，实测通过。

**未知（未核实）**

1. 移动云实例规格与开通进度、新站对外域名最终口径、ICP 备案提交状态：本次未取得相关文件或系统记录。
2. 编校厂商最终选定：方案中仍为四选一待定。
3. `new.gxhczx.gov.cn` 站点的实际用途与内容：仅见配置，未查目录内容。
4. 2021 年之前的历史文章是否另行存放：SQL 导出中最早年份为 2021 年，更早数据去向未知。
