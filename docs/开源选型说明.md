# 河池政协网 · 开源件选型说明

> 编制日期：2026 年 9 月 11 日
> 结论：**不替换现有自研轻量 CMS，只借成熟件的模型与样式**。
> 检索方式：GitHub REST API（`api.github.com/repos/<owner>/<repo>`）与各项目官方文档站点，检索日期 2026 年 9 月 11 日。
> 修订：2026 年 9 月 12 日增补第 3.4 节（正文链路的两份自托管件），并按当日实测修订第 1 节的依赖、规模与检查脚本口径。

## 1. 背景与约束

后台进入“稿库状态机 + 用户分组权限”阶段时，提出的问题是：这块有没有成熟的模板或开源代码可以直接拿来用。

判断必须建立在现有工程约束上，本次确认到的约束：

| 约束 | 事实 |
| --- | --- |
| 技术栈 | PHP 8.5，自写 PSR-4 加载器，**没有 composer.json**：`backend/src/bootstrap.php` 第 5 行写着本期不引 Composer、保持零依赖即可跑（后续要引第三方包时再加 `composer.json`）。2026-09-12 正文链路引入两份**自托管件**（HTMLPurifier、SunEditor）：把发行包直接放进仓库、不改本体，不走 Composer／npm 安装流程，`composer.json` 仍不存在，见 3.4 |
| 现有规模 | 后端 73 个 PHP 文件（`find backend -name '*.php' -not -path '*/vendor/*'`）、前端 199 个文件（`find frontend -type f`），2026-09-12 复核；另有自托管件 `backend/vendor/htmlpurifier` 的 242 个 PHP 文件 |
| 接口契约 | `docs/api-contract.md` 已定版，七个检查脚本在跑（页面 19／20、接口 47、后台 223、排序 19、正文清洗 33＋31、编辑器 55；2026-09-12 复核） |
| 数据 | 旧库 20,705 条历史稿件待迁移 |
| 交付环境 | 移动云自建，政府项目，需要源码可控与依赖可审计 |

结论依据第 2 节的候选件实测数据：本阶段要补的两件事（稿库状态机、用户分组权限）合计约 2—3 天工作量，而换整站框架的迁移成本远高于此。

## 2. 候选件实测数据

星数与最后推送时间均为 2026 年 9 月 11 日通过 GitHub API 观测到的值。

| 项目 | 星数 | 许可 | 最后推送 | 与本案关系 |
| --- | --- | --- | --- | --- |
| tabler/tabler | 41,677 | MIT | 2026-09-10 | 后台 UI 皮，纯 CSS／JS |
| ColorlibHQ/AdminLTE | 45,592 | MIT | 2026-09-10 | 后台 UI 皮，纯 CSS／JS |
| symfony/workflow | 630（主仓 31,155） | MIT | 2026-09-10 | 状态机概念模型（places／transitions／guards） |
| php-casbin/php-casbin | 1,337 | Apache-2.0 | 2026-09-06 | PHP 版 RBAC 引擎 |
| filamentphp/filament | 32,031 | MIT | 2026-09-11 | Laravel 后台面板（需换框架） |
| spatie/laravel-permission | 12,964 | MIT | 2026-09-04 | Laravel 角色权限（需换框架） |
| yangzongzhuan/RuoYi | 8,552 | MIT | 2026-09-07 | 数据权限模型参考（Java） |
| yangzongzhuan/RuoYi-Vue | 3,201 | MIT | 2026-08-26 | 同上，前后端分离版 |
| jeecgboot/JeecgBoot | 47,739 | Apache-2.0 | 2026-09-11 | 低代码平台（Java，依赖重） |
| drupal/core | 194 | GPL-2.0 | 2026-09-11 | 内容审核与工作流（需换框架） |
| wagtail/wagtail | 20,491 | BSD-3-Clause | 2026-09-10 | Django 内容管理（需换语言栈） |
| alphagov/govuk-frontend | 1,452 | MIT | 2026-09-11 | 政务前端设计系统（英国） |
| uswds/uswds | 7,181 | GitHub 未识别出标准许可 | 2026-09-10 | 政务前端设计系统（美国） |
| dedecms/DedeCMS | 154 | GitHub 未识别出标准许可 | 2022-01-28 | **仓库已归档** |
| Pbootcms/Pbootcms | 18 | 无 | 2018-09-10 | GitHub 上的镜像，主库在 Gitee |
| dayrui/xunruicms | 41 | MIT | 2025-11-19 | 迅睿 CMS 的 GitHub 仓库 |

关于国内 CMS 需要说明：织梦、PbootCMS、迅睿这一类的主仓库通常在 Gitee，**GitHub 星数不能代表真实活跃度**，上表的星数只说明“这个 GitHub 仓库现在是这个样子”。织梦的仓库已归档（2022-01-28 之后无推送）。

## 3. 采用结论

### 3.1 借模型，不引依赖

| 要补的部件 | 借用对象 | 落地方式 |
| --- | --- | --- |
| 稿库状态机 | Symfony Workflow | 照它的 places／transitions／guards／metadata 结构写成本项目的 `HechiZx\Content\ArticleWorkflow`，状态与流转集中一处，**不引 Composer 包** |
| 用户分组与数据范围 | 若依 RuoYi | 借“角色按机构划分数据范围”的模型（其 README 第 40 行），落成 `sys_role_channel` 的栏目范围，**代码不通用（Java）** |
| 权限码 | php-casbin | 本项目权限码固定 11 个、角色 4 个，自研 `sys_role_permission` 表即可，不引 RBAC 引擎 |

### 3.2 界面皮后置

Tabler 与 AdminLTE 都是 MIT、纯前端资源，替换 `backend/public/assets/admin.css` 不涉及 PHP 改动。本次**没有做**这一步：稿库与权限的功能改动会连带调整列表、按钮与状态标签，先换皮等于改两遍。等功能稳定后再单独做一次视觉改版。

### 3.3 政务前端设计系统的边界

govuk-frontend 与 USWDS 是英、美两国政府的设计系统，可借的是无障碍规范、组件化拆分与配色对比度做法；**版式不能照搬**，中国政务网站另有版式与标识要求。

### 3.4 正文链路：两份自托管件（2026-09-12 落地）

正文从“18 行 HTML 文本框”换成富文本编辑器，同时补上服务端清洗。两份组件都不属于框架，都按“发行包直接入库、不改本体”的方式接入：

| 组件 | 版本 | 许可 | 落盘位置 | 来源 | 在链路里的位置 |
| --- | --- | --- | --- | --- | --- |
| HTMLPurifier | 4.19.0 | LGPL-2.1 | `backend/vendor/htmlpurifier/`（242 个 PHP 文件） | 官方站点 <http://htmlpurifier.org/>（取自该目录自带的 `README.md`），包名 `ezyang/htmlpurifier` | 白名单清洗的**唯一出口**：保存、内容接口、静态页三处都过它 |
| SunEditor | 3.3.3 | MIT（Copyright (c) 2017-2025 Yi JiHong） | `backend/public/assets/editor/` | npm 包 `suneditor@3.3.3` 的预构建发行物 | 稿件编辑页的富文本编辑器，脚本未加载或初始化失败时退回原 textarea |

三条与第 2 节候选件不同的判断：

1. **许可最省事**：SunEditor 是 MIT，无付费版，发行物里没有“Powered by”一类标识，不存在“要不要花钱去标记”的决策；HTMLPurifier 是 LGPL-2.1，仓库随附 `LICENSE` 与 `CREDITS` 原文，未改其源码。
2. **不引包管理器**：两份都把发行目录直接提交进仓库，第 1 节“没有 composer.json”因此仍然成立。代价是升级要人工换目录，且白名单一旦变化必须同步提升 `backend/src/Content/HtmlSanitizer.php` 的 `DEFINITION_REV`，否则会命中旧的定义缓存。
3. **编辑器不是安全边界**：真正的白名单在后端，编辑器自带的粘贴白名单只是体验层；两边不一致时以服务端为准。

四个候选的剪贴板保真实测、漏洞公告比对，以及 TinyMCE、CKEditor 5、Tiptap 系、Froala 的排除理由，另有一份专项记录，见 [富文本编辑器选型说明（2026-09-12）](富文本编辑器选型说明（2026-09-12）.md)。

## 4. 备选方案与取舍

### 方案二：Laravel + Filament + spatie/laravel-permission

- 收益：后台增删改查、角色权限、表单校验开箱即用，开发速度最快。
- 代价：现有 50 个后端文件全部重写，引入大量 Composer 依赖，与政府项目的依赖审计要求要重新对口径。
- 什么时候是对的：如果项目从零开始，且允许引入框架，这是最省力的起点。

### 方案三：换成 Drupal／Plone／October 一类成品 CMS

- 收益：内容审核、版本、权限、审计全部现成。
- 代价：20,705 条旧数据重新迁移、8 种栏目标版式重做、接口契约重写，还要重新培训使用方。
- 什么时候是对的：如果需求升级成“多单位、多站点、上百账号的集约化平台”，自研轻量 CMS 会到天花板。

## 5. 本次落地结果

按方案一实施后已具备：

1. `database/migrations/{mysql,sqlite}/002_workflow_and_permission.sql`：稿库状态字段、用户分组字段、`sys_role_permission` 表、四个内置角色与权限码、存量 `offline` 归一为 `withdrawn`。
2. `HechiZx\Content\ArticleWorkflow` 与 `HechiZx\Content\Permissions`：状态与权限码集中定义。
3. 后台：稿库导航条（带计数，替代状态下拉）、行内流转动作、编辑页流转区（退回／撤回必填意见）、回收站软删除与恢复。
4. 后台：用户管理、角色与权限（权限码勾选 + 栏目范围勾选）、操作日志页。
5. 权限生效点：稿件新建／编辑／流转、栏目管理、一键发布、用户与角色管理、操作日志，均按权限码校验；数据范围按 `sys_role_channel` 过滤。
6. 正文链路（2026-09-12）：编辑页接入 SunEditor 3.3.3，工具栏含“字体”“字号”“文字颜色”“背景色”；服务端以 HTMLPurifier 4.19.0 作保存、接口、静态页三处唯一清洗出口，白名单以外的标签、事件与协议一律剥离（见 3.4）。

## 6. 本结论失效的条件

- 需求升级为多单位、多站点、上百账号的集约化平台，自研的状态机与权限模型撑不住时，应回到方案三重新评估。
- 团队后续引入 Composer 并接受第三方依赖，Symfony Workflow 与 Casbin 可以直接接管，届时本项目自己的状态机可以平滑替换（接口形状一致）。
- 若维护方没有 PHP 人员，长期维护能力比技术选型更关键，此时应优先考虑成品 CMS 而非自研。

## 7. 参考来源

- Symfony Workflow 官方文档：<https://symfony.com/doc/current/workflow.html>（2026-09-11 访问，页面确认包含 places／transitions／Guard Events／Storing Metadata 等章节）
- Symfony Workflow 仓库 README：<https://raw.githubusercontent.com/symfony/workflow/6.4/README.md>（2026-09-11 访问，自述定位为 managing a workflow or finite state machine，即管理流程与有限状态机）
- 若依 RuoYi README：<https://raw.githubusercontent.com/yangzongzhuan/RuoYi/master/README.md>（2026-09-11 访问，第 40 行“角色菜单权限分配、设置角色按机构进行数据范围权限划分”）
- GitHub REST API 仓库元数据：<https://api.github.com/repos/tabler/tabler> 等同批 20 余个仓库（2026-09-11 观测）
- HTMLPurifier 4.19.0 自托管目录自带文件：`backend/vendor/htmlpurifier/{VERSION,LICENSE,README.md,CREDITS}`（2026-09-12 复核；版本号取自 `VERSION`，许可全文为 LGPL-2.1，官方站点地址取自 `README.md`）
- SunEditor 3.3.3 自托管目录自带文件：`backend/public/assets/editor/LICENSE.txt`（MIT，Copyright (c) 2017-2025 Yi JiHong，2026-09-12 复核）；版本与包来源见 [富文本编辑器选型说明（2026-09-12）](富文本编辑器选型说明（2026-09-12）.md) 的“来源”一节

## 8. 未核实的部分

- drupal.org、octobercms.com、craftcms.com 在本次检索中均未能打开（curl 返回 000 连接失败），这些项目的授权与商用条款未核实。
- Drupal 与 PHP 8.5 的兼容性未验证。
- 织梦（DedeCMS）的商用授权条款未核实。
- 国内 CMS 在 Gitee 的真实活跃度未检索。
- uswds 的许可在 GitHub API 中显示为未识别（NOASSERTION），使用前需查其官方声明。
- 两份自托管件的官网在本次会话中未联网打开，版本与许可信息全部取自仓库内的落盘文件与同目录的选型报告。
- LGPL-2.1 对“发行包入库自托管、未修改本体”这种用法的具体义务，本文件只核对了自带 `LICENSE` 原文，未做法务确认。
