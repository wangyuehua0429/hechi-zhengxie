# 河池政协网 · 后台视觉规范

> 编制日期：2026 年 9 月 11 日
> 修订：2026年9月17日——红色语义收窄到“待办”、概览页改待办优先、行内动作收进“更多”、禁用态与批量提示改观（依据见 `.impeccable/critique/2026-09-17T02-57-22Z__backend-templates-admin.md`）；同日第二轮：全量颜色收进令牌、新增深色模式与主题开关、稿件列表支持跨页选择（整行选中态 + 吸顶批量条）、栏目稿件数改右对齐等宽数字、窄屏侧栏补滑动渐隐；同日第三轮（按两份独立评审的结论）：篮子键改由服务端下发、修掉吸顶包含块与深色填充色对比度、原生控件 `color-scheme` 修正、只在脚本下成立的文案改用 `.js-only`；修掉深色下编辑器输入框1.06:1与纯白纸面、占位文字与激活计数的深色对比度、窄屏“更多”菜单被表格裁掉；同日第四轮：定“按钮与表单标准”（一套三档控件刻度 + 字段解剖五件套 + 勾选项两列栅格 + 只读禁用与反馈规矩），全后台按标准收口
> 适用范围：内容管理后台（`/admin`）。首页管理四页（导航栏目／头条轮换／其他栏目／站内横幅）本轮未逐一改造，但应当按本文的令牌与组件写法对齐。
> 依据：`ui-ux-pro-max` 技能的 “Accessible & Ethical” 风格规则与 dense（8/10）密度取向；技能库给出的藏青蓝配色与 Atkinson Hyperlegible 字体未采纳（与政务红品牌、中文字体栈不适配），只取可访问性与密度规则。

## 1. 设计令牌

全部定义在 `backend/public/assets/admin.css` 的 `:root`，新写的样式从令牌取值，避免再出现一次性数字。

| 类别 | 令牌 | 值 | 说明 |
| --- | --- | --- | --- |
| 品牌 | `--brand` | `#a51d22` | 与站点前台 `--primary` 同值，从后台点开前台不再有色差 |
| 品牌 | `--brand-dark` | `#7f1418` | 悬停、按下 |
| 品牌 | `--brand-soft` | `#faeeee` | 浅底高亮（chip 悬停、子栏目分隔线） |
| 文字 | `--ink` / `--muted` | `#222` / `#5f6771` | `--muted` 在白底 5.73:1，满足 AA |
| 描边 | `--line` / `--field-line` | `#e3e6ea` / `#8a9199` | 表单控件描边 3.19:1，满足非文本对比度 3:1 |
| 间距 | `--space-1..6` | 4／8／12／16／24／32 | 卡片、面板、表单优先用这组 |
| 圆角 | `--radius-sm` / `--radius` / `--radius-lg` | 4／6／10 | 控件／卡片／登录卡 |
| 字号 | `--text-xs..xl` | 12／13／15／17／22 | 正文 15px，次要信息不低于 13px |
| 阴影 | `--shadow-card` / `--shadow-float` | — | 卡片／吸底工具条 |
| 派生 | `--brand-soft-line` / `--brand-soft-strong` | `#f0d3d6` / `#f6dcdf` | 品牌红的浅底描边与深一档悬停底 |
| 派生 | `--danger-line` / `--danger-line-soft` / `--danger-soft` / `--danger-ink`（另有 `--danger-ink-strong`／`--danger-ink-soft` 两档文字色） | `#e0b4ae` / `#f0d6d2` / `#fdeeec` / `#a5301f` | 危险操作的描边、底纹与文字 |
| 派生 | `--warn-line` / `--warn-line-strong` / `--warn-soft` / `--warn-soft-strong` / `--warn-ink` / `--warn-ink-soft` | `#e6cf9a` / `#d29b2c` / `#fff8e1` / `#fffaf0` / `#8a5d0f` / `#8a6d3b` | 警示（非危险）：栏目没选、字段将满、提示条 |
| 派生 | `--soft-tint` / `--field-line-soft` / `--badge-soft` / `--badge-ink` | `#fffafa` / `#cfd4da` / `#ffecec` / `#e60012` | 危险区与批量条激活底、上传框虚线、徽标底色与字色 |
| 表面 | `--surface` / `--surface-2` / `--surface-3` / `--surface-hover` | `#fff` / `#fafbfc` / `#f2f3f5` / `#fafafa` | 卡片与表格／表头与悬停浅底／代码块与中性标签底／侧栏悬停 |
| 表面 | `--brand-tint` / `--on-brand` | `#fdf3f3` / `#fff` | 侧栏当前项与chip悬停的浅红底；实心品牌／危险按钮上的文字 |
| 文字 | `--ink-2` / `--ink-3` / `--ink-ghost` | `#444` / `#55606b` / `#b0b8c1` | 表头与标签／计数标签／空状态图标与分隔符（正文仍是 `--ink`） |
| 文字 | `--brand-ink` / `--err-ink` | `#a51d22` / `#c0392b` | 品牌与危险的**文字角色**：浅色下与填充同色，深色下单独提亮 |
| 状态 | `--ok-soft` / `--ok-ink` / `--ok-line` / `--info-soft` / `--info-ink` | `#eef8f1` / `#1d6b38` / `#cfe7d8` / `#eef3fb` / `#2a5b9e` | 成功与进行中标签、健康横幅 |
| 状态 | `--ok-fill` / `--err-fill` / `--err-fill-strong` | `#1f7a3d` / `#c0392b` / `#a5301f` | 实心填充（上面压白字）：深浅色下都不变；深色里变亮的是“当字”的 `--ok`／`--err` |
| 其他 | `--shadow-elevated` / `--brand-glow` / `--mask-ink` | — | 登录卡投影／登录页顶部品牌光晕／只用给mask渐隐的纯色 |

对比度实测（改动后按 WCAG 相对亮度公式计算）：白字在 `#a51d22` 上 7.5:1、在 `#7f1418` 上 10.42:1；`#a51d22` 在 `#faeeee` 上 6.62:1。

## 2. 红色语义

红色只表达三种意思：**品牌标识**（顶栏标识块、主按钮）、**危险操作**（删除、撤回、红叉）、**需要处理**。概览页因此把“需要处理”做实——**待审／退回**单独一行、有值时数字用 `--brand-ink`，为0时用 `--muted` 降字重并写“暂无待处理”；稿件总数与其余稿库计数一律 `--ink`／`--muted`，收进“按稿库看全部状态”的一行摘要与折叠区。八张卡全标红时红色就不再表示重点，所以红色只给待办，不给“总数”。

深色下的红要分两个角色：**当底**的 `--brand` 取更暗的一档（`#b0343a`，白字6.1:1），**当字**的 `--brand-ink` 反过来提亮（`#f0949a`，深底7.5:1）。浅色下两者同值，所以组件里凡是用红色写字的地方都写 `--brand-ink`，用红色当底的地方才写 `--brand`。

成功与危险同理：**当底的**用 `--ok-fill`／`--err-fill`（深色下也不改值，白字分别5.37:1／5.44:1），**当字的**才用 `--ok`／`--err`（深色下提亮）。第一版把 `--ok`／`--err` 直接拿去当底压白字，深色下只有2.57:1／4.04:1，已改。

## 3. 栏目导航条（两级）

栏目共 43 个，其中 25 个一级项、5 个含子栏目。规则：

- 默认只渲染“全部栏目 + 25 个一级项”（约 2 行），不铺开 43 个 chip。
- 选中项落在某个多子栏目组里时，该组展开显示子 chip；形态仍是导航条，不做下拉。
- **母栏目是开关**：点一次 = 按整组筛选并展开子栏目（母栏目实心红、子栏目浅红底）；已经是整组筛选状态时再点一次 = 收起子栏目、回到“全部栏目”。展开中但未被选中的母栏目用浅红底，与自己的子栏目连成一组。
- **一级项与子栏目分开传参**：一级项走 `?group=<一级栏目号>`，子项走 `?channel=<栏目号>`。原因是“栏目号 401 既是‘政协会议’这一组的组号，又是组内‘全体会议’的号”，只看栏目号无法区分用户想筛整组还是某一个。
- 一级项上的数字是该组各子栏目之和；数字口径与当前列表筛选（稿库 + 关键词）一致。

## 4. 组件约定

| 组件 | 类名 | 约定 |
| --- | --- | --- |
| 状态标签 | `.tag` + `.tag-<状态>` | 胶囊形（`999px`），最小宽度 3.4em，12px |
| 计数标签 | `.vault-num` / `.chip-num` | 小方角 + 描边，与状态标签在形状上区分 |
| 稿库导航 | `.vault-nav` / `.vault-chip` | 胶囊 chip，选中实心品牌色 |
| 栏目导航 | `.channel-nav` / `.channel-chip` / `.channel-chip--group` / `.channel-chip--child` | 一级项加粗并带小三角，子项 13px 且左侧有浅色分隔线 |
| 表格 | `.grid` | 行悬停浅底 `#fafbfc`，空结果行不随鼠标变色 |
| 空状态 | `.empty-icon` / `.empty-title` / `.empty-hint` / `.empty-action` | 图标 + 说明 + 主行动按钮 |
| 提示条 | `.flash` + `.flash-icon` | 左侧色条 + 图标，`role="status"`／`role="alert"` |
| 预览框 | `.slot-preview` / `.slot-preview-note` | 站内横幅的图片位预览。2026-09-12 新增“选后即时预览”：预览图由脚本写入 `data:` 地址（后台 CSP 的 `img-src` 只放行 `'self'` 与 `data:`，`blob:` 会被拦），底部 `.slot-preview-note` 提示条用 `#fff8e1` 底 + `#8a6d3b` 字 + 上描边，提示文案见模板 `backend/templates/admin/banners.php` 的 `data-preview-pending` 一行 |
| 提交中 | `.btn.is-busy` | 改文案为“处理中…”并压暗，**不使用 `disabled`** |
| 行内“更多” | `.row-more` / `.row-more-menu` | 原生 `details`／`summary`，没有脚本也能展开；一行里只留“编辑”一个主动作，预览／复制链接／流转／删除都进菜单。桌面端菜单留在正常流（展开把行撑高，换来永不被表格容器裁切）；窄屏（≤720px）改绝对定位浮层——留在正常流会参与表格最小宽度计算、展开一次整张表就宽一截，而且固定列会把菜单下半截盖住，所以那一档还要给展开行的单元格抬 `z-index` |
| 批量自检提示 | `.bulk-error` | 落在批量条内的错误提示（`role="alert"`），**不用原生弹窗**；提示后把焦点移到出问题的控件 |
| 禁用控件 | `.btn[disabled]` / `.btn.is-disabled` | **不靠半透明**：描边、底纹、文字各降一档，仍保持5:1以上（半透明时文字只剩2.76:1） |
| 概览待办 | `.stats-todo` / `.stat--todo` / `.stat-hint` / `.stats-more` | 待审／退回一行、待办标红；其余状态进“按稿库看全部状态”一行摘要 + 折叠区 |
| 读屏播报 | `.visually-hidden` + `role="status"` | 复制链接这类没有页面跳转的动作，用一条隐藏的`live region`播报“链接已复制” |
| 跨页选择 | `.bulk-form.is-sticky` / `.bulk-scope` / `tr.is-selected` | 勾选记在 `sessionStorage`，键由服务端按归一化后的筛选算好写在 `data-bulk-key` 上（前端不解析URL——站内各条链接的查询串顺序与空值都不一样，自己算会把篮子算丢）；不在本页的id用隐藏域 `ids[]` 补进表单；有选中时批量条吸顶（类名挂在 `<form>` 上，见第5节第4条）、整行染浅红、条内给出“本页N篇，另M篇在其它页” |
| 深浅色开关 | `.theme-toggle` + `data-state` | 顶栏右侧一枚30×30按钮，两个内联SVG按 `data-state` 显示其一；没有脚本时不渲染（模板带 `hidden`），交给系统主题 |
| 侧栏滑动提示 | `.sidenav.is-scroll-start` / `.is-scroll-middle` / `.is-scroll-end` | 只在窄屏生效，右（或左右两侧）还有内容时用 `mask-image` 渐隐；类名由脚本按 `scrollLeft` 切换 |
| 数字列 | `td.num` / `th.num` | 右对齐 + `font-variant-numeric: tabular-nums`；统计数字、计数标签、日期、分页也统一用等宽数字 |
| 菜单动效 | `.row-more[open] > summary > span` | “更多”的小三角展开时转180°，`.15s`，`prefers-reduced-motion` 下关掉 |
| 只在脚本下成立的文案 | `.js-only` | 例如批量条的“翻页不会丢掉已选”——跨页保留是脚本给的，没有脚本时这段字直接不出，别承诺做不到的事。用法与既有的 `html.js`／`html:not(.js)` 一致 |

## 5. 四个必须遵守的坑

1. **提交中的按钮不能 `disabled`。** 新建稿件的“保存并发布／保存为草稿”与稿库流转按钮都靠 `name/value` 传参，按钮被禁用后参数不会提交，服务端就收不到 `status`／`action`。统一改用 `aria-busy` + `.is-busy` + `pointer-events:none`。
2. **图标一律内联 SVG。** 项目无 `composer.json`、无 npm 依赖，内网部署也不能依赖外部图标 CDN；统一放在 `backend/templates/admin/_icons.php`，用 `require` 取数组（不是定义函数，避免同请求内重复声明）。
3. **深色令牌写了两份，必须同步。** 一份在 `@media (prefers-color-scheme: dark)` 的 `:root:not([data-theme="light"])` 里，一份在 `:root[data-theme="dark"]` 里（前者管跟随系统，后者管用户手动覆盖）；改一处就要改另一处。另外主题引导必须走外链脚本 `assets/theme.js`——后台CSP的 `script-src` 只放行登录页那一个内联脚本哈希，再加内联脚本会让登录页之外的主题引导被浏览器直接拦掉。
4. **`position: sticky` 要挂在包含块够高的那一层。** 批量条一开始把 `is-sticky` 加在 `.bulk-bar` 上，而它的包含块就是那根条子本身（可偏移量恒为0），等于没吸；现在类名挂在 `<form class="bulk-form">` 上——它的包含块是整页的 `.main`，才真的吸得住。同理，`.form-actions` 能吸底是因为父级是整张长表单。

## 6. 深色模式

- **怎么选主题**：默认跟随系统（纯CSS，首帧就正确）；顶栏那枚开关把选择写进 `localStorage.hechi-admin-theme`，`assets/theme.js`在 `<head>` 里同步读出来写 `<html data-theme>`，所以刷新、跳页都不会闪一下浅色。
- **组件不用改**：所有颜色都走令牌，深色只重定义 `:root` 的那一组值（含投影、品牌光晕、表单描边）。新增组件时如果又写了裸十六进制颜色，深色一定穿帮。
- **对比度实测（深色，按WCAG相对亮度公式在渲染页上量）**：正文14.89、次要文字6.54、品牌红链接7.49、“已发布”标签8.36、实心红chip上的白字6.18、禁用按钮5.58、表头9.32——全部≥4.5:1。
- **原生控件也要跟着变**：`:root` 只写 `color-scheme: light`，系统深色由媒体查询、用户手选深色由 `data-theme="dark"` 改成 `dark`；写 `light dark` 会让“系统深色＋用户选浅色”时下拉弹层、滚动条仍是深色（实测已修正：显式浅色时 `color-scheme` 为 `light`）。
- **图片类内容不调色**：深色下站内横幅、头条缩略图仍是原图，不做降亮处理（内容真实优先）。
- **第三方编辑器也要接管**：`assets/editor/admin-editor.css` 里用 `.writing-paper .sun-editor(.sun-editor-editable)`重定义了SunEditor自带的那批变量（`--se-main-color`、`--se-edit-background-color` 等）。不接管的话，输入框底色跟着令牌变深，编辑器却把文字写死成浅色下的深灰——2026-09-17实测原题三列与来源四个输入框只有1.06:1、正文纸面还是纯白。该文件委员门户也用，所以每个 `var()` 都带原值兜底（那边是 `--m-*` 令牌）。
- **占位文字**：`admin.css` 里 `input::placeholder／textarea::placeholder` 统一切到 `--muted`，浏览器默认的 #757575深底上只有3.63:1。

## 7. 按钮与表单标准（2026-09-17定）

这一节是硬约定：后台任何新页面、新表单都按它写，不再各页各造一套。

### 7.1控件刻度：一套三档，不许再造尺寸

| 档位 | 高度 | 字号 | 用在哪 | 类名 |
| --- | --- | --- | --- | --- |
| 小 | 28px（`--control-h-sm`） | 13px | 表格行内动作、徽标下拉、密集工具条、图标按钮 | `.btn-sm`（图标另加 `.btn-icon`） |
| 默认 | 34px（`--control-h`） | 14px | 表单字段、筛选器、页面上的普通按钮与主按钮 | `.btn` / `.btn-primary` |
| 大 | 40px（`--control-h-lg`） | 15px | 一页只有一个主动作的地方（登录提交、确认页的危险按钮） | `.btn-lg` |

- 表单控件（`input`／`select`／`textarea`／文件框）默认走34px，密集区（表格里的下拉）走28px；多行文本最小72px。**文件框要连原生按钮一起对齐**（`::file-selector-button` 给高度），否则框是34px、里面的按钮还是25px。
- 分页器整行走默认档：翻页链、页码与“跳到”都是34px——同一行里28与34混排看着高低不齐。
- 窄屏（≤720px）整条刻度抬到触控尺寸：小档40、默认与大档44、字段44、页码40。这是同一套刻度的第二档，不是另造一套。
- 选择器要写 `.btn.btn-sm` 这种两个类的形式：元素限定选择器（如 `.pick-list button`）的权重比单个类高，只写 `.btn-sm` 会在某些页面被压回14px。
- 实施前实测过30／34／38／44／46／48／71六种高度混在同一批页面里；现在只允许上表三种。**表外只留四个有理由的例外**：顶栏“退出”27px（文字链接，命中区≥24）、深浅色开关30×30、轮播缩略图上的删除✕26px（伪元素把命中区补到36）、编辑器工具栏32px（第三方suneditor自带）。

### 7.2字段解剖：五件套

**标签 → 控件 → 提示 → 必填标记 → 字段级错误**，顺序固定：

| 部位 | 类名 | 规格 |
| --- | --- | --- |
| 标签 | 容器内的文字（或 `.field-label`） | 14px、`--ink-2`；组内标签统一13px加粗 |
| 控件 | 直接写 `input`／`select`／`textarea` | 见7.1 |
| 提示 | `.field-hint`（旧的 `.muted`／`.hint-line` 同样收口） | 12px、`--muted`、行高1.7 |
| 必填 | `.field-req` | 标签后加红色星号 |
| 字段错误 | `.field-error` + 控件加 `aria-invalid="true"` | 12px、`--err-ink`；控件描边转红并带一圈浅红底 |

- 所有表单容器共用同一套排布：`.filters > label`、`.edit-form label`、`.filter-field`、`.writing-field`、`.login-field`、`.field`——纵向、间距4px、字号14px，不再各写各的。
- 宽度分档：`.field--xs`88／`.field--sm`140／`.field--md`220／`.field--lg`320／`.field--full`占满。**短枚举与数值字段必须收窄**（状态、排序号、条数、页码），长文本才用full。
- 字段组：`.edit-form .row` 是自适应栅格、列宽封顶260px并左对齐（`minmax(200px, 260px)`），组内自动折行；`.full` 与 `.field--full` 横跨**整个字段组**（不是整个页面宽度）。字段自己有宽度档，列再无限拉宽只会在右侧留一大片空。
- 窄屏（≤720px）栅格回到单列铺满（`minmax(0, 1fr)`）：260px 的列宽上限在手机上会把字段关在左半边，`.grow` 的 `span 2` 还会多挤出一条隐式轨道。
- 组级提示必须贴在它所解释的那一行下面（`.edit-form p.muted` 的上外边距是负的；选择器**不加 `>`**——提示常写在 `<section class="card">` 里，直系匹配会漏掉一半页面）——两条一样的空当会让读者分不清提示属于上面还是下面那组。
- **长提示不要塞进带宽度档的 `<label>`**：宽度档只管控件，提示会跟着被压窄折成好几行（2026-09-17实测“首页显示条数”那格因此从112px长到171px）。提示挪到字段组之后、用 `.field-hint` 铺满整行。

### 7.3勾选项：两列栅格，不要居中竖排

一个选项一行，`.check-line`：左列复选框（16×16）、右列名称，灰色说明换行到第二列（`.muted` 落在 `grid-column: 2`）。成组用 `fieldset.check-set` + `legend`。

- 表格里的勾选格（稿件列表的批量选择）用 `.bulk-cell` 把复选框包起来，整格可点：窄屏40×40、桌面28×28。`input` 本身只有13×13，不做这层手机上戳不准。
- `.check-line` 全文**只允许定义一处**（就是本节这条）。同一个选择器在文件后面再写一遍 `display: flex`，会把网格顶回去——导航栏目页与删除确认页此前就是这么跟其它页跑偏的。

> 反面例子：2026-09-17之前，`.edit-form label` 的字段解剖（纵向排列）权重压过 `.check-line`，角色多选被挤成**居中竖排**，一屏占掉半页还读不出勾的是哪个。

### 7.4只读与禁用

`disabled`／`readonly` 一律降一档底纹 + `--muted` 文字 + `not-allowed` 光标。同一页里“看得见但改不了”的字段（栏目号、URL标识）必须写成 `disabled`，不能只靠旁边的提示文字说明。

### 7.5交互反馈

| 场景 | 约定 |
| --- | --- |
| 焦点 | 控件用“品牌色描边 + 3px浅底环”；链接与按钮沿用全局 `:focus-visible` 的3px描边 |
| 悬停／按下 | 悬停只变描边与底纹（不跳字号、不位移）；按下再暗一档 |
| 提交中 | `.is-busy` 改文案“处理中…”、**不 `disabled`**（见第5节第1条） |
| 校验失败 | 字段级：`aria-invalid="true"` + `.field-error`；页面级：`.flash`（`role="status"`／`role="alert"`），焦点落到出问题的控件 |
| 危险动作 | 只用 `--err-fill` 实心 + 二次确认，不用大红色铺满整块 |

## 8. 本轮未做

- 首页管理四页未按本规范逐页调整（那边有并行改动，避免冲突）。2026-09-12 的站内横幅出图建议与选图预览只按上表补了组件约定，未做整页视觉重排。
- 主题只有“跟随系统 + 一键切换”，没有第二级选项（固定浅色／固定深色／定时切换）；也没做打印样式（后台偶尔要打列表，打印仍按浏览器默认）。
- 深色下没有单独校对图片类内容的观感（横幅、头条缩略图按原图出）。
- 后台是同步渲染，暂不做骨架屏／加载占位；若以后改成异步列表，这条要补。
- 批量提交被服务端拒绝（权限、CSRF、状态不合法）时，篮子已经在提交那一刻清空了，用户得重新勾一遍；要改成“服务端确认成功后再清”，需要在回跳地址上带一个信号。
- 主题与侧栏展开态存在 `localStorage` 里，但没监听 `storage` 事件——同一浏览器开两个标签页时，另一个要刷新才跟上。
- 界面的脚本级行为（跨页篮子、主题开关、Shift区间选择、吸顶）目前只有标记级断言兜底，没有浏览器自动化用例；要不要为后台引入浏览器测试，留着决定（本机是 `playwright-cli`，不是零成本）。
- **字段级校验还没接线**：`.field-error`／`.field-req`／`aria-invalid="true"` 的样式已经备好、名册表头也已用上必填星号，但服务端校验退回时仍然只给页面级 `.flash`，没有把错误标到具体字段、也没有把焦点送过去。要落地得动控制器与模板的校验分支，单独立项。
