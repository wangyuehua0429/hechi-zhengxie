# 河池政协网 · 开发与架构说明

> 依据《河池政协网开发思路与技术栈方案》（2026-09-01）整理，随实现演进更新。当前以 `frontend/home/` 前端静态版为实际交付，本文构建为规划中的目标架构。
>
> **当前实现状态（2026-09-12）**：
> - 前端：`frontend/home/` 已完成首页 `index.html`、栏目页 `channel.html`、详情页 `detail.html` 及配套 CSS／JS／数据快照。
> - 后端：`backend/` 已从空骨架变为**可运行的最小骨架**——本文第 2 节的数据模型落到 `database/migrations/{mysql,sqlite}/` 的 001—004 四个迁移（19 张业务表 + 迁移记录表），第 3 节的“内容/资源 API 服务”与“发布与页面引擎”已有实现（`backend/public/index.php`、`routes/api.php`、`src/Api`、`src/Publish`），数据取自 MySQL／SQLite 而非静态快照，接口契约见 [api-contract.md](api-contract.md)。
> - 后台（2026-09-11 补齐、2026-09-12 增补）：登录、用户与角色、按“角色 × 栏目”的数据范围、栏目管理、稿件增删改查、稿库六态与流转、附件与正文插图上传、操作日志均已实现；2026-09-12 起正文用富文本编辑器（SunEditor 3.3.3）并以 HTMLPurifier 4.19.0 作唯一清洗出口，编辑页收敛为写作窗（原标题三项、摘要移出到首页轮换头条），首页管理补置顶／高亮／徽标与预览／复制链接。稿库方案见 [稿库与内容状态设计](稿库与内容状态设计.md)，权限方案见 [用户分组与权限设计](用户分组与权限设计.md)，编辑器方案见 [富文本编辑器选型说明（2026-09-12）](富文本编辑器选型说明（2026-09-12）.md)。
> - 旧地址 301（2026-09-12 新增）：`sys_url_redirect` 映射生成、Nginx 片段／核对 CSV 产出、PHP 入口的 301 判定与命中计数已实现（`backend/bin/redirects.php`、`src/Publish/RedirectMap.php`、`src/Http/LegacyRedirect.php`），规则与未覆盖项见 [旧地址301映射说明](旧地址301映射说明.md)；旧库全量导入仍属阶段 D。
> - 旧库迁移（2026-09-12 新增）：`tools/migrate/` 已落地两段式迁移（解析清洗 → 入库）与抓图工具，口径、基线与验收见 [旧库迁移说明](旧库迁移说明.md)；已按 4 月 28 日导出实跑验证（迁移 4,490 篇、发布 2,714 篇详情静态页、301 映射 8,301 条），待拿到重新导出的全量后重跑。
> - 仍为空的：`services/proofreader/`（编校微服务）、`api/`（预留独立部署位）。
> - 两处第三方件按“发行包入库、不改本体”自托管：`backend/public/assets/editor/`（SunEditor 3.3.3，MIT）与 `backend/vendor/htmlpurifier/`（HTMLPurifier 4.19.0，LGPL-2.1），取舍与来源见 [开源选型说明](开源选型说明.md) 第 3.4 节。
> - 本地开发用 SQLite 即可跑通接口与发布（`php backend/bin/migrate.php` → `seed.php` → `php -S`），生产切 MySQL；进度、待办与口径冲突见 [下阶段开发计划（2026-09-11）](下阶段开发计划（2026-09-11）.md)。

## 1. 系统分层

```
┌─ 前端层：主站响应式门户（PC/手机/微信），全文静态化页面 ＋ 预留子站皮肤接口（本期：frontend/home）
├─ API 层：内容/资源 REST API（前台、后台、校对、未来多端共用的数据出口）
├─ 应用层：PHP CMS（栏目/文章/附件管理、发布调度、静态化生成、URL 重写；多站点 site_id 预留）
├─ 服务层：智能编校微服务（本期，独立 Python 部署）· 图片管理平台（独立项目，可后置）
├─ 数据层：MySQL（开发+生产统一，本期不切国产库）＋ Redis 缓存 ＋ 对象存储 ＋ 静态文件存储（Nginx 直出）
└─ 基础层：移动云 IaaS ＋ 安全组/WAF/备份/监控
```

## 2. 数据模型（新库，规划）

核心原则：**内容/资源统一入库、先入库后使用**，以 API 为数据出口；栏目、文章、附件、专题统一元数据模型（site_id / channel_id / 元数据 / 发布时间 / 公开范围）。

| 表 | 关键字段 | 说明 |
| --- | --- | --- |
| sys_site | site_id、站点标识、域名、模板主题、状态 | 多站点预留，本期只启用主站 |
| sys_channel | channel_id、site_id、父级、栏目名、栏目类型、URL 别名、排序、权重 | 支持 17 个一级栏目与二级子栏目 |
| cms_article | article_id、site_id、channel_id、标题、原标题（引题/主标题/副题）、正文、摘要、作者、来源、发布时间、置顶/头条、URL 别名、发布状态、公开范围 | 已实现稿库六态（草稿/待审/退回/已发布/已撤回/回收站）与流转留痕，见 [稿库与内容状态设计](稿库与内容状态设计.md)；`orig_kicker`／`orig_title`／`orig_subtitle` 由迁移 004 加入 |
| cms_article_channel | article_id、channel_type、排序、是否主栏目、置顶、高亮、徽标文字 | 一篇稿可挂多个栏目，各栏目分别置顶；高亮＝首页标题正红加粗，徽标＝标题后小标记，只在首页管理维护 |
| cms_attachment | article_id、存储 key、类型、大小、下载计数 | 历史图片附件迁移后挂接 |
| sys_url_redirect | 旧路径 → 新路径 | 301 映射，保 SEO 与外链 |
| sys_user / sys_role / sys_role_permission / sys_role_channel | 权限码 + 按栏目的数据范围 | 四个内置角色（栏目编辑／审核／发布／管理员）；稿件终审口径仍按合同既定安排执行 |
| cms_article_sync | 上/下行稿件互通 | 预留，依赖县区子站开通 |
| sys_operation_log | 操作/登录日志 | 留存 180 天 |
| 编校扩展 | 文章编校状态/编校单编号；独立编校词库表（领导、行政区划、忌讳词、本地提法） | 本期使用 |

## 3. 模块划分

| 模块 | 内容 | 阶段 |
| --- | --- | --- |
| 内容管理后台 | 统一后台、栏目/模板管理、基础发布状态、富文本编辑器（**已实现**：SunEditor 3.3.3 + HTMLPurifier 4.19.0 清洗） | 一期 |
| 发布与页面引擎 | 首页/栏目/详情全文静态化生成、增量发布、缓存、站点地图、301 重定向 | 一期 |
| 内容/资源 API 服务 | 栏目/文章/附件/专题统一 REST 出口 | 一期 |
| 无障碍与适老化 | 接入政务无障碍组件或开发等价方案 | 一期 |
| 多站点管理 | 子站开通、模板皮肤、域名绑定 | 预留升级接口 |
| 稿件互通 | 双链路上行/下行 | 预留 |
| 数据迁移工具 | 旧库导出、清洗去重、图片附件迁移、URL 映射生成 | 一期 |
| 统计看板 | 访问量/来源/栏目稿量 | 一期平台层 |
| 智能编校模块 | 编辑器侧栏嵌入厂商 API + 本地词库 + AI 兜底，生成《智能编校单》 | 本期，独立 Python 服务 |
| 图片管理平台 | 上传-检索-下载-管理五模块，OCR 预留 | 独立项目，可后置 |

## 4. 关键技术点

1. **数据库方言迁移**：本期生产统一 MySQL；SQL 层按方言适配隔离，为日后切换达梦/金仓预留平滑通道。
2. **URL 301 映射**：迁移脚本按旧库文章 ID 批量生成映射表，Nginx/PHP 层统一 301。
3. **全文静态化与增量发布**：发布即生成静态 HTML，内容变更触发对应页增量刷新；静态文件由 Nginx 直出。
4. **内容/资源 API 契约**：前台/后台/校对/多端统一走 REST，先定义契约与版本再实现前端。
5. **多站点预留**：nginx virtualhost + 单 CMS 多站点（site_id 维度），本期只建主站。
6. **编校模块边界**：以独立 Python 服务接入，接口契约与字段先行，不与 CMS 核心耦合。
7. **无障碍与安全合规**：接入政务无障碍组件；落实等保二级（前后台分机部署、日志 ≥6 个月、敏感词过滤、防篡改/防攻击/防病毒）；补齐 HTTPS 与站点地图/网站标识。
8. **数据安全**：涉密稿件不发外网、不调用厂商接口；本地词库存政协自有服务器；迁移全程保密协议 + 多重备份。

## 5. 实施节奏

一期（主站 + 校对模块）9/1 启动。停服口径以《河池政协网迁移建设和技术维护合同（9 月 3 日）》为准：**11 月 20 日起关停旧政务云**，新站 9 月 30 日前完成自查迁出；方案书与执行时间表所载 11 月 1 日停服口径与合同不一致，待甲方确认（详见 [下阶段开发计划（2026-09-11）](下阶段开发计划（2026-09-11）.md) 第二节）。校对模块以独立 Python 服务同步推进，采用“接口契约先行、并行开发、联调联测”。若工期紧张，以主站先上线为优先，校对作为一期紧后段收尾。子站与稿件互通本期不做，按预留接口后续开通。
